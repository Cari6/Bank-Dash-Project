import { StylesProps } from "../../utils/styles-props";

export const styles = (): StylesProps => ({
  itemListContainer: {
    display: "flex",
    cursor: "pointer",
    position: "relative",
    height: "60px",
    paddingLeft: "40px",
    alignItems: "center",
  },
});
